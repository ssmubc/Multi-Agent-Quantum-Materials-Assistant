# Configuration Template
# DO NOT store actual credentials in this file
# Use AWS Secrets Manager or environment variables for production

# AWS Configuration
AWS_REGION=us-east-1
# Use AWS_PROFILE instead of access keys for security
AWS_PROFILE=your_profile_name

# Application Configuration
BRAKET_DEFAULT_DEVICE_ARN=arn:aws:braket:::device/quantum-simulator/amazon/sv1
BRAKET_WORKSPACE_DIR=./quantum_workspace

# For production: Store sensitive values in AWS Secrets Manager
# Materials Project API: materials-project/api-key
# Cognito credentials: /quantum-matter/cognito/*